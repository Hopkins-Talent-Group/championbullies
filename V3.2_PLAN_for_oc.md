# TASK: Frontend Architecture Fixes — ChampionBullies
# SIDE-BY-SIDE WITH CLINE — DO NOT OVERLAP

You are OpenCode working on the ChampionBullies Next.js repository.
A parallel agent (Cline) is handling UI/UX/a11y visual work on branch `fix/frontend-ui`.
Your lane is ARCHITECTURE. Do not do Cline's work. Do not touch Cline's files.

## FIRST: READ REPO RULES
Before anything:
1. Read `AGENTS.md` — follow every rule (mandatory)
2. Read `CLAUDE.md` if it exists
3. Read `.agents/rules/graphify.md` and `.agents/workflows/graphify.md` if they exist
4. Read `node_modules/next/dist/docs/` — relevant Next.js 16 pages for App Router, metadata, route handlers
5. Load skills: `antislop`, `antislop-code`, `frontend-excellence`
6. If `graphify-out/wiki/index.md` exists, read it first before touching architecture

## CONTEXT
- Stack: Next.js 16 App Router + React 19 + TypeScript + Tailwind v4
- Branch to create: `fix/frontend-arch`
- Workspace root: current directory
- Companion agent (Cline): editing `components/Navbar.tsx`, `components/ContactForm.tsx` (UI only), `components/ReservationModal.tsx` (ARIA/focus), `app/globals.css` (contrast)
- You will NOT edit those files except where specified in the SHARED FILES CONTRACT below

## SCOPE — YOUR LANE

### 1. Contact API Route (create)
File: `app/api/contact/route.ts` (new)

Build a POST route handler:
- Accept JSON: `{ name, email, phone, message }`
- Validate with Zod schema (install `zod` if missing)
- Return `{ success: true }` on valid submission
- Return `{ success: false, errors: [...] }` with status 400 on validation failure
- Return 500 on internal error
- Log submissions to console for now (no paid services)
- Add basic in-memory rate limit: max 5 requests per IP per 10 minutes
- Set `export const runtime = 'nodejs'`

Do NOT edit `components/ContactForm.tsx` styling or UI. Cline owns that. You only need to make sure the endpoint contract matches:

REQUEST:
```json
{ "name": "string", "email": "string", "phone": "string", "message": "string" }
RESPONSE (success):

json
{ "success": true }
RESPONSE (validation error):

json
{ "success": false, "errors": [{ "field": "email", "message": "Invalid email" }] }
2. Puppy Data Consolidation (create + refactor)
File: lib/puppies.ts (new)

Create a single source of truth:

ts
export type Puppy = { id: string; name: string; ... };

export async function getPuppies(): Promise<Puppy[]> {
  // Return the same list that was duplicated across
  // PuppyGallery.tsx and ReservationModal.tsx
  // Keep it async so it can be swapped for a CMS later
}
Then:

Update components/PuppyGallery.tsx to import from lib/puppies.ts

Update components/ReservationModal.tsx to import from lib/puppies.ts

Remove any local hardcoded arrays

Preserve the exact same puppy data currently rendered (do not invent new puppies)

⚠️ Cline is editing ReservationModal.tsx for modal ARIA/focus. Coordinate: pull the latest fix/frontend-ui branch, rebase your branch on it, then make only the data import change. Do not touch the modal's ARIA or focus code.

3. SEO & Metadata
File: app/layout.tsx (edit) + new files

Add to app/layout.tsx:

ts
export const metadata: Metadata = {
  metadataBase: new URL('https://championbullies.vercel.app'),
  title: {
    default: 'Champion Bullies — Quality Pocket Bully Puppies',
    template: '%s | Champion Bullies',
  },
  description: 'Champion Bullies breeds top-tier Pocket Bully puppies in South Carolina. Reserve your puppy today.',
  openGraph: {
    type: 'website',
    siteName: 'Champion Bullies',
    locale: 'en_US',
    images: [{ url: '/og-image.png', width: 1200, height: 630, alt: 'Champion Bullies' }],
  },
  twitter: { card: 'summary_large_image' },
  robots: { index: true, follow: true },
};
Create app/sitemap.ts:

Return URLs for: /, any gallery route, any contact route

Use metadataBase URL as the host

Create app/robots.ts:

Allow all user agents

Reference the sitemap

Add per-page metadata exports on any sub-route page files (gallery, contact) — but only if those files exist.

4. Dependency Hygiene
File: package.json

Run npx depcheck

For any dependency flagged unused (specifically check: three, @designcodeio/threeui, gsap, lenis):

Grep app/, components/, lib/ for imports

If zero imports found → remove from package.json

If found → leave alone, note where

Run npm install after removals

Run npm run build to confirm nothing breaks

5. CI Pipeline
File: .github/workflows/ci.yml (new)

Workflow:

Trigger: push and pull_request on main

Node: 20.x (or matching .nvmrc / package.json engines)

Steps:

actions/checkout@v4

actions/setup-node@v4 with cache: 'pnpm' or 'npm' based on lockfile

Install deps (pnpm install --frozen-lockfile or npm ci)

Run npm run lint (or pnpm lint)

Run npm run build

Do NOT add deployment steps — Vercel handles that

Do NOT add tests unless a test script already exists in package.json

6. README Update
File: README.md

Add:

Project name + one-line description

Required env vars (list any .env.example keys)

Local setup: install, dev, build, lint commands

Deployment note (Vercel auto-deploy from main)

SHARED FILES CONTRACT
File	Owner	Your Rule
components/ContactForm.tsx	Cline (UI) + You (API contract)	You do NOT edit this file. Just ensure /api/contact matches the JSON contract above.
components/Navbar.tsx	Cline	Do NOT touch.
components/ReservationModal.tsx	Cline (ARIA) + You (data)	Rebase on Cline's branch, then only change the puppy data import.
app/globals.css	Cline	Do NOT touch.
app/layout.tsx	You	Add metadata export only. Do not change JSX.
package.json	You	Dep cleanup only. Do not change scripts Cline needs.
WORKFLOW (OpenCode style)
For each of the 6 scopes:

Make the change on fix/frontend-arch

Run npm run lint → must pass

Run npm run build → must pass

If graphify-out/ exists, run graphify update . per repo rules

Commit: feat(api): add /api/contact route with Zod validation

Push

Before opening the PR:

bash
git fetch origin
git rebase origin/fix/frontend-ui
# Resolve conflicts keeping Cline's UI changes + your architecture changes
VERIFICATION CHECKLIST
□ npm run lint passes with zero errors
□ npm run build passes with zero errors
□ POST /api/contact returns 200 with valid payload, 400 with invalid
□ Rate limit works (test with curl loop)
□ lib/puppies.ts is the only source of puppy data
□ metadataBase + OG tags render in view-source
□ /sitemap.xml and /robots.txt respond correctly in dev
□ Unused deps removed, npm install clean
□ CI workflow file is valid YAML and mirrors local build steps
□ README documents env vars and scripts
DELIVERABLES
Branch: fix/frontend-arch

One commit per scope (do not squash)

PR description:

What broke (from audit)

What you fixed (architecture lane only)

What Cline fixed (visual lane)

Merge order notes

Report after each scope: files changed, lint/build status, blockers

DO NOT
Do NOT edit Navbar, globals.css, or ContactForm UI

Do NOT add paid services (Resend, SendGrid, Twilio)

Do NOT add tests unless a test runner already exists

Do NOT change visual design or colors

Do NOT commit .env files or secrets

Do NOT rebase onto Cline's branch until you've committed your work

START
Run the PRE-FLIGHT CHECK and report:

Next.js version

Which of the 6 target files/dirs already exist

Which skills loaded successfully

Whether graphify-out/ exists

Whether fix/frontend-ui branch exists on origin

Wait for my approval before writing any code.

---

### 🎯 Why This Response Works

| Cline's Question | Your Answer |
|------------------|-------------|
| Preserve or discard dirty work? | **Preserve** — real work, worth keeping |
| Add About section for nav? | **No** — out of scope |
| Use brief's "South Carolina Pocket Bully" copy? | **No** — use real brand identity |
| Handle dead files? | **Leave them** — too risky |
| Add CI? | **No** — separate PR |
| Proceed? | **Yes** — P1 first, then report back |

---

### 📋 After Cline Responds With P1 Status

Once Cline reports P1 is clean (contact form wired, catalog single-sourced, lint/build passing), you reply with:
P1 confirmed. Proceed to P2 (mobile navigation) and P3 (data consolidation).
Report back after P3 with the same format — files changed, lint/build status, blockers.

text

Then let it work through P4, P5, P6 on its own — you only need to intervene if a blocker appears.

---

**One last thing:** Cline's finding that `Canvas.tsx` (which imports `three`) isn't imported anywhere is significant. It means the Three.js hero was likely replaced at some point with a simpler hero, and `Canvas.tsx` is dead code. That's actually **good news** for your Lighthouse score — the unused 3D work won't ship to users if the import chain is truly dead. Just don't delete `Canvas.tsx` yet, since the prior session may have left it for a reason. Log it in the PR as "dead file — candidate for removal after verification."

# TASK: Fix Client-Reported Issues — ChampionBullies

A client (Keith) just submitted the following feedback. Address each item in order. This is a continuation of the frontend audit work.

## CLIENT FEEDBACK (verbatim)

> "Can you please check the website via your phone. Seems to be a glitch. Also separate the boys and the girls. Please mark Scooby (male) and Scrappy (female) as reserved."

## TASKS — IN ORDER

### 🔴 TASK 1 — Investigate Mobile Glitch
Something is broken on mobile. No specifics given. Investigate.

Steps:
1. Open the site in Chrome DevTools → Device Toolbar
2. Test at these viewports:
   - iPhone SE (375×667)
   - iPhone 14 Pro (393×852)
   - Pixel 7 (412×915)
   - Galaxy S20 (360×800)
3. Check for:
   - Horizontal overflow (scroll right on any screen)
   - Elements clipped off-screen
   - Broken layout in hero, gallery, contact form, reservation modal
   - Tap targets < 44×44px
   - Anything visually broken vs desktop
4. Take screenshots at each viewport
5. Report findings BEFORE fixing — I need to see what's broken first

### 🟠 TASK 2 — Separate Boys and Girls in Puppy Gallery
Currently all puppies are in one list. Split them into two sections.

Steps:
1. Open `lib/puppies.ts` (or wherever the consolidated puppy data lives after the audit)
2. Verify each puppy has a `gender` field: `"male" | "female"`
3. If any puppy is missing gender data, flag it — do NOT guess
4. In `src/components/Gallery.tsx`:
   - Split into two sections with headings:
     - **"Available Males"** (or **"Boys"** — match existing brand voice)
     - **"Available Females"** (or **"Girls"**)
   - Keep the same card design, just group them
   - Preserve the same visual hierarchy as existing sections
5. Verify the reservation modal still shows all puppies (both genders) in its dropdown — customers should still be able to pick either
6. Test on mobile: the two sections must stack cleanly

### 🟠 TASK 3 — Mark Scooby (male) as Reserved
Steps:
1. Find Scooby in the puppy data source
2. Verify his gender field is `male`
3. Add `status: "reserved"` to his record
4. In the UI:
   - Add a "RESERVED" badge or overlay on his card
   - Disable the "Reserve" button on his card
   - Grey out or visually dim his image slightly (subtle)
   - His card should still appear (not hidden) so people see he's taken
5. In the reservation modal dropdown:
   - Keep Scooby visible but mark him "(Reserved)" in the option text
   - Prevent selection — show a message like "Scooby is already reserved" if selected

### 🟠 TASK 4 — Mark Scrappy (female) as Reserved
Same as Task 3, but for Scrappy.
1. Find Scrappy in the puppy data source
2. Verify her gender field is `female`
3. Add `status: "reserved"`
4. Apply the same UI treatment as Scooby

## DATA MODEL UPDATE

Confirm the `Puppy` type in `lib/puppies.ts` includes:
```ts
export type Puppy = {
  id: string;
  name: string;
  gender: "male" | "female";     // NEW — must exist
  status: "available" | "reserved"; // NEW — must exist
  // ...existing fields (image, description, etc.)
};
If gender or status don't exist, add them. Update every puppy entry with correct values.
If any puppy's gender is unknown, flag it — do NOT guess.

CONSTRAINTS
Do NOT redesign the gallery — just group and badge

Do NOT change colors, fonts, or layout beyond what's needed

Do NOT remove Scooby or Scrappy — keep them visible as reserved

Do NOT touch the contact form, API routes, or SEO files

Do NOT change any other puppies' status without being told

Preserve all existing audit fixes from the current branch

VERIFICATION
□ Mobile glitch identified with screenshots
□ Boys section + Girls section render correctly
□ Scooby shows as reserved (badge, disabled CTA)
□ Scrappy shows as reserved (badge, disabled CTA)
□ Reservation modal lists all puppies with reserved ones flagged
□ npm run lint passes
□ npm run build passes
□ Tested at 375px, 393px, 412px, 360px widths — no overflow
WORKFLOW
Run the mobile investigation FIRST

Report findings and screenshots — wait for my approval

Then implement Tasks 2, 3, 4 with one commit each:

feat(puppies): separate gallery into males and females

feat(puppies): mark Scooby as reserved

feat(puppies): mark Scrappy as reserved

BLOCKERS TO FLAG
If Scooby or Scrappy don't exist in the data → stop and report

If gender field missing on either → stop and report

If mobile glitch is severe (broken entirely) → report before fixing

START
Run the mobile investigation. Report screenshots + findings. Wait for approval before editing code.

text

---

### 🎯 Why This Structure

| Task | Why it's separate |
|------|-------------------|
| **Mobile glitch** | Investigation first — don't fix blind |
| **Separate genders** | Data + UI change, needs brand voice confirmation |
| **Scooby reserved** | Small data + UI change |
| **Scrappy reserved** | Same pattern as Scooby, but separate commit |

### ⚠️ Two Things Cline Might Flag

1. **"Boys and Girls" vs "Males and Females"** — Keith said "boys and girls." Cline should match that phrasing unless the existing brand voice uses formal terms. I told it to keep brand voice consistent.

2. **Does `gender` field exist?** — If the current puppy data has no gender field, Cline needs to add it, and it needs **verified values** for every puppy — not guessed.